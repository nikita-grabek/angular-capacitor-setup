import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.phatasiename.app',
  appName: 'phantasiename',
  webDir: 'dist/phantasiename',
  server: {
    androidScheme: 'https'
  }
};

export default config;
